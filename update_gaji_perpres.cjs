const admin = require("firebase-admin");

// Configure this line with your actual service account key if running outside the browser
// OR we can write a browser-based script if you're executing it from the app.
// Since you are running local node.js, I will use a simple client-side firebase script via node for quick execution using your existing SDK configs.
